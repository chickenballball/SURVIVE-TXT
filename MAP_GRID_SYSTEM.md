# MAP_GRID_SYSTEM.md — 地圖網格系統

---

## 🗺️ 系統概述

每一關由 **12 個小地圖** 組成，地圖位於 **6x6 = 36** 的網格中隨機生成。

---

## 📐 基本結構

```
       X軸 (6個)
    ┌─────────────────┐
    │ □  □  □  □  □  □ │  ← 可選位置 (36個)
    │ □  □  □  □  □  □ │
 Y軸 │ □  □  □  □  □  □ │  (6個)
(6個)│ □  □  □  □  □  □ │
    │ □  □  □  □  □  □ │
    │ □  □  □  □  □  □ │
    └─────────────────┘
    
    每次遊戲隨機選擇 12 個位置
    連成一條從起點到終點的路徑
```

---

## 🚪 連接系統

每個小地圖有 **4 個邊緣**：

```
        [上邊緣]
            
[左邊緣]  □  [右邊緣]
            
        [下邊緣]
```

### 邊緣類型

| 邊緣類型 | 說明 |
|----------|------|
| **出口** | 通往下一個小地圖 |
| **入口** | 從上一個小地圖來 |
| **封閉口** | 不是出口，可能是：強怪物 / 寶箱 / 陷阱 |
| **牆壁** | 完全封閉，什麼都沒有 |

### 連接規則

```
┌─────────────────────────────────────┐
│ 規則                                 │
├─────────────────────────────────────┤
│ 1. 除了終點，每個地圖至少有：          │
│    - 1 個出口                        │
│    - 1 個入口                        │
│                                     │
│ 2. 終點只有入口，沒有出口             │
│                                     │
│ 3. 起點只有出口，沒有入口             │
│                                     │
│ 4. 封閉口（不是出口的牆壁）可能：       │
│    - 強怪物守門                       │
│    - 寶箱                             │
│    - 陷阱                             │
│    - 隱藏捷徑                          │
└─────────────────────────────────────┘
```

---

## 🗺️ 地圖生成算法

### 步驟 1：隨機選擇位置

```python
def select_map_positions():
    # 在 6x6 網格中隨機選擇 12 個位置
    all_positions = [(x, y) for x in range(6) for y in range(6)]
    selected = random.sample(all_positions, 12)
    return selected
```

### 步驟 2：確保連通

```python
def ensure_connectivity(selected_positions):
    # 使用 Flood Fill 檢查所有位置是否連通
    # 如果不連通，重新選擇
    while not is_connected(selected_positions):
        selected = random.sample(all_positions, 12)
    return selected
```

### 步驟 3：確定起點和終點

```python
def determine_start_end(positions):
    # 起點 = 最左側/最上方的選中格子
    start = min(positions, key=lambda p: (p[0], p[1]))
    
    # 終點 = 最右側/最下方的選中格子
    end = max(positions, key=lambda p: (p[0], p[1]))
    
    return start, end
```

### 步驟 4：決定連接方向

```python
def determine_connections(positions, start, end):
    # 確保從起點到終點的路徑是連通的
    # 每個地圖連接到最近的鄰居
    
    connections = {}
    for pos in positions:
        neighbors = get_neighbors(pos)
        valid_neighbors = [n for n in neighbors if n in positions]
        connections[pos] = valid_neighbors
    
    return connections
```

### 步驟 5：設置封閉口

```python
def set_blocked_exits(positions, connections):
    # 對於每個地圖，隨機封閉一些不是出口的邊緣
    for pos in positions:
        if is_end_point(pos):
            continue  # 終點不封閉
        
        # 隨機決定哪個邊緣是封閉口
        blocked_edges = random.choice([
            "怪物",   # 強怪物守門
            "寶箱",   # 寶箱房
            "陷阱",   # 陷阱
            "捷徑",   # 隱藏路徑
            None      # 正常出口
        ])
```

---

## 📊 地圖連接示意圖

### 示例：Stage 1 工廠

```
        [上] [上] [上] [上] [上] [上]
       ┌─────────────────────────┐
   [左]│ S  □  □  □  □  □  □  □ │[右]
       │ ↓  ↙  ↓  ↘  □  □  □  □ │
       │ □  □  □  □  □  □  □  □ │
       │ ↓  ↙  ↓  ↘  □  □  □  □ │
       │ □  □  □  □  □  □  □  □ │
       │ ↓  ↙  ↓  ↘  □  □  □  □ │
   [左]│ □  □  □  □  □  □  E  □ │[右]
       └─────────────────────────┘
        [下] [下] [下] [下] [下] [下]

S = 起點 (左上角)
E = 終點 (右下角)
↓ ↙ ↘ = 地圖之間的連接方向
```

### 封閉口示例

```
某個地圖的4個邊緣：

    [上]: 正常出口 → 下一個地圖
    [下]: 入口 ← 上一個地圖
    [左]: 封閉口 → 菁英怪物 (擊敗獲得寶箱)
    [右]: 正常出口 → 下一個地圖
    
    或者：
    
    [左]: 封閉口 → 寶箱房
    [右]: 封閉口 → 陷阱
```

---

## 🎁 封閉口獎勵類型

| 封閉口類型 | 內容 | 說明 |
|------------|------|------|
| **菁英怪物** | 擊敗後獲得紫色裝備 | 難度較高 |
| **寶箱** | 隨機道具/金幣/武器 | 需要鑰匙 |
| **陷阱** | 立即觸發陷阱效果 | 可躲避 |
| **隱藏捷徑** | 開啟通往其他區域的快速通道 | 戰略價值 |
| **商店** | 用TOKEN購買道具 | 隨機出現 |
| **事件** | 隨機事件 (Buff/DeBuff) | 增加變數 |

---

## 🏁 地圖類型

### 1. 普通地圖 (Normal)
```
- 包含 3-5 種普通敵人
- 1 個 BOSS
- 1-3 個封閉口
```

### 2. 商店地圖 (Shop)
```
- 沒有敵人
- 有商人 NPC
- 可以購買道具/武器
- 2-4 個封閉口 (通常是正常出口)
```

### 3. 寶箱地圖 (Treasure)
```
- 1 個菁英怪物守護
- 1 個大寶箱
- 1 個封閉口
- 1 個出口
```

### 4. 事件地圖 (Event)
```
- 沒有敵人
- 有隨機事件發生
- 事件內容：
  - Buff: 全屬性+20%
  - DeBuff: 敵人攻擊力x2
  - 商人: 特殊商品
  - 祭壇: 永久強化
```

### 5. 休息地圖 (Rest)
```
- 沒有敵人
- 可以回滿 HP
- 重置所有技能冷卻
- 1 個封閉口
```

### 6. BOSS 地圖 (Boss)
```
- 只有 BOSS
- 沒有封閉口 (終點)
- 擊敗後通往下一個 Stage
```

---

## 📋 地圖生成配置文件

```yaml
# 每個 Stage 的地圖配置

Stage_1:
  total_maps: 12
  grid_size: [6, 6]
  normal_maps: 8      # 普通地圖
  shop_maps: 1       # 商店
  treasure_maps: 1    # 寶箱
  event_maps: 1       # 事件
  boss_map: 1         # BOSS地圖
  
  enemies_per_map:
    min: 3
    max: 5
    
  blocked_exits:
    min: 0
    max: 2
    
  blocked_exit_rewards:
    elite_monster: 0.4   # 40% 機會
    treasure: 0.3        # 30% 機會
    trap: 0.2            # 20% 機會
    shortcut: 0.1         # 10% 機會
```

---

## 🔄 隨機事件系統

每個地圖可能有隨機事件：

| 事件 | 效果 | 發生率 |
|------|------|--------|
| **雙倍金幣** | 擊殺敵人金幣x2 | 10% |
| **敵人暴走** | 敵人攻擊力x1.5但移動更慢 | 15% |
| **道具風暴** | 地圖上隨機出現道具 | 20% |
| **商人突襲** | 商人出現提供特殊商品 | 10% |
| **寧靜時刻** | 敵人不動5秒 | 15% |
| **幸運降臨** | 掉落率x2 | 15% |
| **無事件** | 正常遊戲 | 15% |

---

## 🎮 玩家視角體驗

### 地圖切換流程

```
┌─────────────────────────────────────┐
│                                     │
│   [當前地圖完成 / 到達終點]          │
│                                     │
│         ↓                           │
│                                     │
│   ┌─────────────────┐              │
│   │   顯示下一地圖   │              │
│   │   S ───→ □      │              │
│   │      □          │              │
│   │                 │              │
│   │   [上] [左][右]  │              │
│   │   顯示可選路徑   │              │
│   └─────────────────┘              │
│                                     │
│         ↓                           │
│                                     │
│   玩家可以選擇下一個前進方向         │
│   不同的方向可能通向：               │
│   - 正常地圖                       │
│   - 封閉口 (怪物/寶箱)              │
│   - 捷徑                          │
│                                     │
└─────────────────────────────────────┘
```

### HUD 顯示

```
┌─────────────────────────────────────┐
│ HP: ████████░░ 80%                  │
│                                     │
│ 第 5/12 地圖          TOKEN: 1,234 │
│                                     │
│       ┌─────────────────┐           │
│       │    [你目前在這]   │           │
│       │        □        │           │
│       │      ↗ ↑ ↖      │           │
│       │    ← □ + □ →     │           │
│       │      ↘ ↓ ↙      │           │
│       │        □        │           │
│       │       ...       │           │
│       └─────────────────┘           │
│                                     │
│ 選擇方向: 上/下/左/右                │
└─────────────────────────────────────┘
```

---

## 📁 下一步

1. **每個 Stage 的具體地圖設計**
2. **地圖內部障礙物配置**
3. **BOSS 房間特殊機制**
4. **地圖之間的視覺過渡**

準備好繼續哪個部分？